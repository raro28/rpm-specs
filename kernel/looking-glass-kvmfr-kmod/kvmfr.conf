# Static device size in MiB. Must be >= width * height * 4 * 2 + 10 MiB.
# 256 MiB matches the looking-glass shmem size in win11-system's libvirt XML
# and is enough for any guest resolution up to 4K.
options kvmfr static_size_mb=256
